var breakfastItems = ['oatmeal', 'cereal','toast', 'blueberries'];

var ticketPrices = [15, 30, 45, 60];

var correctAnswer = [ (1 < 2), (5 < 7), (5 < 3), (10 < 7)];

// This method will remove the last element from the end

breakfastItems.pop();

// This method will method only include the numbers at index 1 and 2

ticketPrices.slice();

// This method will remove the first element from the beginning of the array

correctAnswer.shift();

// An array contains a specific index position for each element beginning at 0
// To access each value, you call the variable name follwed by [index position]
// Using the above breakfastItems example to get to cereal

breakfastItems[1];
